Work directly inside this project folder and complete the task. Do not only explain what I should do.

Create, run, debug, and validate a Python script named:

`build_ntw3_army_builder_database.py`

Keep the script in the project root so I can rerun it later.

## Folder structure

The project folder looks like this:

```text
project_folder/
├── build_ntw3_army_builder_database.py
├── tsv info files/
│   ├── localisation.loc.tsv
│   ├── mp_general_command_ratings.tsv
│   ├── ntw3_battle_entities.tsv
│   ├── ntw3_land_projectiles.tsv
│   ├── ntw3_land_units.tsv
│   ├── ntw3_unit_stats_land.tsv
│   ├── units_to_exclusive_faction_permissions.tsv
│   └── gun_type_to_projectiles.tsv
└── icons/
    └── many unit icon files, mostly .tga
```

Use paths relative to the script itself:

```python
PROJECT_ROOT = Path(__file__).resolve().parent
TSV_DIR = PROJECT_ROOT / "tsv info files"
ICON_DIR = PROJECT_ROOT / "icons"
```

Do not depend on the terminal’s current working directory.

## Required workflow

1. Inspect the actual headers and representative rows of every TSV before deciding the joins.
2. Create the Python script.
3. Run it.
4. Fix any parsing, encoding, duplicate-key, column-name, localisation, merge, icon, speed, or projectile errors.
5. Generate all required output files.
6. Validate representative infantry, grenadier, skirmisher, cavalry, artillery, howitzer, general, commander, and ToW units.
7. Do not stop after writing the script.
8. Confirm that the output files were successfully created.

## Output files

Create these in the project root:

* `ntw3_army_builder_units.csv`
* `ntw3_merge_warnings.csv`
* `ntw3_merge_summary.txt`

Do not modify any source TSV or icon file.

## Purpose

The CSV is for an external NTW3 army-builder.

Produce one row per:

`unit_key + faction_key`

A unit must appear multiple times when it belongs to multiple faction or army-corps keys.

Use:

`units_to_exclusive_faction_permissions.tsv`

as the source of faction and army-corps availability.

Only include rows where:

`allowed == true`

## Exact final CSV columns

Use this exact order:

1. `unit_key`
2. `faction_key`
3. `unit_name`
4. `unit_class`
5. `men_raw`
6. `men_display`
7. `speed_code`
8. `speed_entity_key`
9. `division_brigade_code`
10. `division_id`
11. `brigade_id`
12. `base_mp_cost`
13. `unit_cap`
14. `range`
15. `weapon_key`
16. `projectile_key`
17. `range_selection_method`
18. `command_stars`
19. `is_general`
20. `is_commander_variant`
21. `is_tow_variant`
22. `icon_name`
23. `icon_filename`
24. `icon_path`
25. `icon_match_method`

Do not include long descriptions or unrelated combat statistics.

## Input parsing

Read all eight TSV files as tab-separated UTF-8 text.

Skip metadata rows beginning with `#`.

Treat blank strings as null values rather than zero.

Preserve keys exactly.

Do not strip, merge, or collapse `_com_`, `_tow_`, or other variants.

## Core joins

Use:

```text
ntw3_land_units.key
=
ntw3_unit_stats_land.key
```

Use:

```text
units_to_exclusive_faction_permissions.key
=
ntw3_land_units.key
```

Use:

```text
mp_general_command_ratings.unit_key
=
ntw3_land_units.key
```

Use:

```text
units_to_exclusive_faction_permissions.faction
```

as:

```text
faction_key
```

A missing command-rating row must produce blank `command_stars`, not zero.

## Unit name

Resolve the real player-facing unit name from:

`localisation.loc.tsv`

Inspect the actual localisation-key patterns rather than relying on one guessed prefix.

Use the unit key and the `on_screen_name` field from `ntw3_land_units.tsv` as lookup candidates.

The final `unit_name` should match the name shown in the game’s hover popup.

If a name cannot be resolved:

* leave it blank;
* log a warning;
* do not generate a name from the unit key.

## Men count

Use the `men` field from `ntw3_unit_stats_land.tsv`.

Set:

```text
men_raw = raw database value
men_display = men_raw / 2
```

When a General-class commander variant has no direct stats row, remove its trailing
`_com_<digits>` suffix and inherit `men_raw` from that regular base unit. When a
`_gen_staff_` card still has no source count, use 32 raw men so its final displayed
count remains the verified 16. If duplicate base-unit stats disagree on `men`, use
the first declared source row deterministically.

Keep both fields.

If dividing by two produces a non-integer result, preserve it and log it for review.

## Speed classification

Do not output numeric movement speed.

Output the in-game-style speed code.

Derive the code only from the actual battle-entity key assigned to that unit.

Relevant fields may include:

* `man_entity`
* `mount_entity`
* `articulated_entity`

Match those references against:

`ntw3_battle_entities.key`

Use this exact mapping:

```text
inf_gren_v1   -> G1
inf_gren_v2   -> G2
inf_gren_v3   -> G3
inf_gren_v4   -> G4
inf_gren_v5   -> G5
inf_gren_v6   -> G6

inf_line_v1   -> L1
inf_line_v2   -> L2
inf_line_v3   -> L3
inf_line_v4   -> L4
inf_line_v5   -> L5
inf_line_v6   -> L6

inf_skirm_v1  -> S1
inf_skirm_v2  -> S2
inf_skirm_v3  -> S3

inf_skirmg_v1 -> GS1
inf_skirmg_v2 -> GS2
inf_skirmg_v3 -> GS3

cav_v1        -> C1
cav_v2        -> C2
cav_v3        -> C3
cav_v4        -> C4
cav_v5        -> C5

art_foot_v1   -> F1
art_foot_v2   -> F2
art_foot_v3   -> F3
art_foot_v4   -> F4
art_foot_v5   -> F5
art_foot_v6   -> F6

art_horse_v1  -> H1
art_horse_v2  -> H2
art_horse_v3  -> H3
```

Do not infer speed from:

* unit name;
* unit category;
* unit class;
* unit-key text;
* historical unit type.

Some grenadier units use `inf_line_v#`, so they must receive an `L#` speed code rather than a `G#` code.

### Choosing the entity

Use these rules:

1. For infantry, grenadiers and skirmishers, use `man_entity` when it matches one of the mapped infantry entities.
2. For cavalry, use whichever entity-reference field directly contains `cav_v1` through `cav_v5`.
3. For artillery, use whichever entity-reference field directly contains `art_foot_v1` through `art_foot_v6` or `art_horse_v1` through `art_horse_v3`.
4. Inspect the actual data instead of assuming the correct field solely from its name.
5. Save the matched entity in `speed_entity_key`.
6. If no mapped entity exists, leave `speed_code` blank and log the unresolved entity.

Do not invent codes for unrelated entities such as:

* `gun_crew`
* `gun_crew_horse`
* `cav_gen`
* `camel`
* `artillery_fixed`

## Division and brigade

The localised description contains a hidden tag:

```text
ACDV<division>B<brigade>
```

Example:

```text
ACDV3B2
```

means:

* division `3`
* brigade `2`

Resolve the description internally only to extract this tag.

Do not include the full description in the final CSV.

Create:

```text
division_brigade_code = ACDV3B2
division_id = 3
brigade_id = 2
```

Preserve the raw division number.

Do not add one.

For a genuine `ntw3_ac_*` army-corps faction, apply the final-division convention
when a non-general card has no tag:

* foot and fixed artillery join the foot-artillery brigade of the highest numbered division;
* horse artillery joins the horse-artillery brigade of that division;
* skirmishers, sappers, and marines join that division's final brigade.

Use a narrowly scoped verified override when every card in a final division lacks a
tag and the division number therefore cannot be inferred from other cards. Leave
staff generals and non-army-corps records blank when no tag exists.

## Cost and cap

Use:

```text
base_mp_cost = multiplayer_cost
```

from `ntw3_land_units.tsv`.

Use:

```text
unit_cap = total_cap_mp
```

from `ntw3_land_units.tsv`.

Do not calculate:

* XP-adjusted cost;
* brigade discounts;
* division discounts;
* final army cost.

## General fields

Set:

```text
command_stars
```

from `mp_general_command_ratings.tsv`.

Set:

```text
is_general
```

primarily from the actual unit class.

Set:

```text
is_commander_variant = true
```

when the key contains `_com_`.

Set:

```text
is_tow_variant = true
```

when the key contains `_tow_`.

Keep commander variants and base units as separate records.

## Range

The final `range` must come from:

```text
ntw3_land_projectiles.effective_range
```

Every nonblank range must be supported by an exact projectile-key match.

### Infantry and cavalry missile weapons

Inspect fields such as:

* `primary_missile_weapon`
* `default_missile_type`
* other missile or projectile references

If a field directly matches:

```text
ntw3_land_projectiles.key
```

use it.

Save:

* original weapon reference as `weapon_key`
* matched projectile as `projectile_key`
* `effective_range` as `range`
* `range_selection_method = direct_projectile_match`

Do not use fuzzy or approximate matching.

### Artillery chain

Use:

```text
unit gun type
→ gun_type_to_projectiles.gun_type
→ selected shot_type
→ ntw3_land_projectiles.key
→ effective_range
```

In `gun_type_to_projectiles.tsv`, `shot_type` is the projectile key.

Save:

* unit gun type as `weapon_key`
* selected `shot_type` as `projectile_key`

### Ordinary cannons

For ordinary cannons, select the projectile ending in:

```text
_shot
```

This includes gun types such as:

* `cannon_*`
* `ott_cannon_*`
* `siege_cannon_*`

Set:

```text
range_selection_method = cannon_round_shot
```

Do not select:

* `_cannister`
* `_canister`
* `_shrapnel`

For special shrapnel-capable cannons such as:

```text
cannon_6_pounder_Britain_Shrapnel
```

still use the ordinary round-shot projectile.

### Howitzers

For howitzers in default mode, select the projectile ending in:

```text
_shell
```

This includes:

* `howitzer_*`
* `ott_howitzer`

Set:

```text
range_selection_method = howitzer_shell
```

### Unicorns

Treat unicorns as howitzer-style weapons for the requested range.

For gun types beginning with:

```text
unicorn_
```

choose the projectile ending in:

```text
_shell
```

Set:

```text
range_selection_method = unicorn_shell
```

### Mortars

For mortar gun types, choose the projectile ending in:

```text
_shell
```

This includes:

* `mortar_*`
* `ott_mortar`

Set:

```text
range_selection_method = mortar_shell
```

Do not choose quicklime or round shot.

### Rockets

For rocket gun types, choose the exact projectile:

```text
rockets
```

when available.

Set:

```text
range_selection_method = rocket_projectile
```

### Range safety rules

* Never join a unit key directly to a projectile key.
* Never select the highest-range projectile just because it is highest.
* Never use fuzzy matching.
* If multiple candidates match the required rule, log the ambiguity and leave the range blank.
* If no valid projectile exists, leave `range` and `projectile_key` blank.
* Every nonblank range must come from an exact row in `ntw3_land_projectiles.tsv`.

## Icon matching

Recursively scan:

```text
PROJECT_ROOT / "icons"
```

Support at least:

* `.tga`
* `.png`
* `.jpg`
* `.jpeg`
* `.webp`

Use the `icon_name` field from `ntw3_land_units.tsv` as the primary reference.

Create:

* `icon_name`
* `icon_filename`
* `icon_path`
* `icon_match_method`

Matching may be case-insensitive, but preserve the real filename spelling.

### Matching priority

Try these exact methods in order:

1. exact filename using `icon_name` if it already includes an extension;
2. exact filename stem using `icon_name`;
3. exact filename using `icon_name + ".tga"`;
4. exact filename using `unit_key + "_icon.tga"` only if it exists;
5. exact filename using `unit_key + ".tga"` only if it exists.

Do not:

* fuzzy-match;
* rewrite prefixes;
* assume `ntw3` should become a faction name;
* choose arbitrarily between multiple matches.

For a successful match:

```text
icon_filename = actual filename including extension
```

```text
icon_path = path relative to the project root using forward slashes
```

Example:

```text
icons/austria_cav_heavy_019_010_0023_com_1007_icon.tga
```

Set `icon_match_method` to one of:

* `icon_name_exact_filename`
* `icon_name_exact_stem`
* `icon_name_added_tga`
* `unit_key_icon_exact`
* `unit_key_exact`

If unresolved or ambiguous, leave the icon path fields blank and log the issue.

## Duplicate handling

Some TSV exports may contain duplicate keys.

If duplicate rows are completely identical, deduplicate them.

If rows sharing a key differ:

* do not arbitrarily choose one;
* record every conflicting row in the warning file;
* stop if the conflict makes the final output ambiguous.

Prevent accidental many-to-many joins.

The final CSV must be unique by:

```text
unit_key + faction_key
```

## Sorting

Sort the final CSV by:

1. `faction_key`
2. `division_id`
3. `brigade_id`
4. `unit_class`
5. `unit_name`
6. `unit_key`

Save it as UTF-8 with BOM for Excel compatibility.

## Warning file

Create structured warning columns such as:

* `warning_type`
* `unit_key`
* `faction_key`
* `source_file`
* `source_row`
* `reference_value`
* `details`

Include warning types such as:

* `duplicate_conflict`
* `missing_unit_name`
* `missing_land_stats`
* `missing_division_tag`
* `unmapped_speed_entity`
* `unresolved_weapon`
* `unresolved_gun_type`
* `ambiguous_projectile`
* `missing_projectile`
* `missing_icon`
* `ambiguous_icon`

## Validation summary

Write into `ntw3_merge_summary.txt`:

* project-root path;
* TSV-directory path;
* icon-directory path;
* row count for every input TSV;
* unique key count for each relevant TSV;
* duplicate count per file;
* icon count by extension;
* final output row count;
* unique final unit-key count;
* unique faction-key count;
* resolved and unresolved unit-name counts;
* count of units with `ACDV` tags;
* count without `ACDV` tags;
* count of resolved speed codes;
* count of each speed code;
* list of unmapped speed entities;
* count of resolved ranges;
* ranges grouped by selection method;
* unresolved weapon keys;
* unresolved gun types;
* missing projectile keys;
* ambiguous projectile selections;
* count of units with command stars;
* resolved, unresolved and ambiguous icon counts;
* icon matches grouped by method;
* duplicate conflicts;
* whether any merge unexpectedly multiplied records.

## Required assertions

Assert that:

* `tsv info files` exists;
* `icons` exists;
* all eight TSV files exist;
* metadata rows beginning with `#` were removed;
* every output row has a nonblank `unit_key`;
* every output row has a nonblank `faction_key`;
* only `allowed == true` rows are included;
* final rows are unique by `unit_key + faction_key`;
* every nonblank speed code came from the explicit entity mapping;
* no speed code was inferred from the unit name or class;
* every nonblank range came from an exact projectile-table match;
* cannon ranges use `_shot`;
* howitzer, unicorn and mortar ranges use `_shell`;
* every nonblank icon path points to an existing file;
* every icon path is relative;
* no icon or projectile was selected using fuzzy matching.

Use:

* Python 3
* pandas
* pathlib
* regular expressions

Organize the code into small functions with clear comments.

At the end:

1. print the paths of the three generated files;
2. print a concise validation summary;
3. tell me which mappings remain unresolved;
4. do not claim the database is complete if warnings remain.
